{
  "name": "cmd-riker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:focused": "node --test",
    "test": "node --test tests/*.test.ts",
    "build": "tsc -p tsconfig.build.json",
    "build:local-release": "node scripts/build-local-release.mjs",
    "start": "node dist/lead-agent/cli.js",
    "lifecycle": "node src/lifecycle-cli.ts",
    "live-smoke": "node dist/lead-agent/live-smoke.js"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.84.3",
    "@earendil-works/pi-ai": "0.84.3",
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "5.9.2"
  }
}
